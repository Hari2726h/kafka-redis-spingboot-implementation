# Demo Frontend

Requirements: Node.js and npm/yarn.

Run locally (dev):

```bash
cd frontend
npm install
npm run dev
```

This will start the Vite dev server on port 3000 and proxy `/api` to `http://localhost:8080`.

To build a production bundle:

```bash
npm run build
npm run preview
```
